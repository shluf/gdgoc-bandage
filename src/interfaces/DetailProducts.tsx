export interface DetailProduct {
    id: string
    name: string
    price: number
    inStock: boolean
    rating: number
    reviewCount: number
    description: string
    images: string[]
    colors: string[]
}